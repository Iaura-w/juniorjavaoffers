# Mongo Properties
spring.data.mongodb.uri=mongodb://localhost:${DB_PORT:27017}/offers
# Mongock Properties
mongock.change-logs-scan-package=com.javaoffers.mongo.config

scheduling.enabled=false

redis.cache.enabled=false